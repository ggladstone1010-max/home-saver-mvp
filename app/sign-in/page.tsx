"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {Logo} from "@/components/app-shell";import {Icon} from "@/components/icons";import {useDemo} from "@/components/demo-provider";import {defaultState} from "@/lib/demo";
export default function SignIn(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const{replace}=useDemo(),router=useRouter();const enter=()=>router.push("/dashboard");const submit=(e:FormEvent)=>{e.preventDefault();if(!/^\S+@\S+\.\S+$/.test(email)||!password){setError("Enter a valid email and password.");return}enter()};const demo=()=>{replace({...defaultState,completed:true,onboardingStep:6,fund:{...defaultState.fund,interested:true,submitted:true},broker:{...defaultState.broker,mode:"introduction",consent:true}});enter()};return <main className="auth-page"><div className="auth-panel"><Logo/><span className="eyebrow">Welcome back</span><h1>Sign in to Home Saver</h1><p>No real authentication is used in this demonstration.</p><form onSubmit={submit}><label className="field"><span>Email</span><div><input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div></label><label className="field"><span>Password</span><div><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div></label>{error&&<p className="form-error">{error}</p>}<button className="button button-primary wide" type="submit">Sign in <Icon name="arrow"/></button></form><button className="button button-secondary wide demo-user" onClick={demo}>Continue as demo user</button><small>New to Home Saver? <Link href="/onboarding">Create account</Link></small></div></main>}
