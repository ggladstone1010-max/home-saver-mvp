import type {PropertyInput} from "@/lib/types";
export const PROPERTY_CSV_COLUMNS=["inventory_type","market_status","title","address","suburb","state","postcode","price_guide","mpg_floor_price","developer_name","developer_project_name","bedrooms","bathrooms","car_spaces","property_type","new_or_established","expected_market_date","auction_date","hero_image_url","description","is_active"] as const;
export function parsePropertyCsv(text:string){const lines=text.trim().split(/\r?\n/).filter(Boolean),headers=lines[0]?.split(",").map(x=>x.trim())??[];const missing=PROPERTY_CSV_COLUMNS.filter(x=>!headers.includes(x));if(missing.length)return{valid:[] as PropertyInput[],errors:[`Missing columns: ${missing.join(", ")}`]};const errors:string[]=[],valid:PropertyInput[]=[];lines.slice(1).forEach((line,index)=>{const values=line.split(",").map(x=>x.trim().replace(/^"|"$/g,"")),row=Object.fromEntries(headers.map((h,i)=>[h,values[i]??""]));if(!row.title||!row.address||!row.suburb||!row.price_guide){errors.push(`Row ${index+2}: title, address, suburb and price are required`);return}valid.push({inventory_type:row.inventory_type as PropertyInput["inventory_type"],market_status:row.market_status as PropertyInput["market_status"],title:row.title,address_line_1:row.address,address_line_2:null,suburb:row.suburb,state:row.state as PropertyInput["state"],postcode:row.postcode,latitude:null,longitude:null,price_guide:Number(row.price_guide),mpg_floor_price:row.mpg_floor_price?Number(row.mpg_floor_price):null,developer_name:row.developer_name||null,developer_project_name:row.developer_project_name||null,listing_agent_name:null,listing_agent_company:null,expected_market_date:row.expected_market_date||null,auction_date:row.auction_date||null,expected_availability_date:null,bedrooms:Number(row.bedrooms),bathrooms:Number(row.bathrooms),car_spaces:Number(row.car_spaces),land_size:null,property_type:row.property_type,new_or_established:row.new_or_established as "New"|"Established",description:row.description,hero_image_url:row.hero_image_url||null,image_urls:[],access_level:"Standard",is_active:row.is_active.toLowerCase()!=="false",is_featured:false,can_make_offer:row.inventory_type==="PRE_MARKET_MPG",can_request_inspection:true,can_register_floor_interest:row.inventory_type==="LIVE_MPG",source_partner_id:null,external_reference:null,internal_notes:null,created_by:"csv-import"})});return{valid,errors}}
